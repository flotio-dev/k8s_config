---
kubeconfig_owner: "{{ ansible_user | default('root') }}"
kubeconfig_group: "{{ ansible_user | default('root') }}"
kubernetes_join_token_ttl: "{{ lookup('ansible.builtin.env', 'KUBERNETES_JOIN_TOKEN_TTL') | default('2h', true) }}"
