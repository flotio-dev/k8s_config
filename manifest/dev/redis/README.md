# Redis (Production)

Redis cache deployment for Flotio production environment.

## 🛠️ Configuration
- **Service**: ClusterIP service for internal caching.
- **Persistence**: Standard Redis persistence configuration.

## 🚀 Deployment
Deployed to the `prod` namespace.
